# Deliberately inert LIVE boundary. The pinned execution runtime rejects LIVE at
# startup and no static exchange credential is mounted. A future managed KMS/Vault
# design must replace this file after an independent LIVE-readiness review.
services:
  execution-engine:
    environment:
      KAIROS_TRADING_MODE: LIVE
