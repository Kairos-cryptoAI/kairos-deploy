# This is topology-only input. It must never contain a credential.
# The operational wrapper requires explicit paths and creates no secret files.
KAIROS_OFFLINE_OUTBOX_DRAIN_SECRETS_DIR=./secrets-offline-outbox-drain-DO-NOT-CREATE
KAIROS_OFFLINE_OUTBOX_DRAIN_INPUT_DIR=./offline-outbox-drain-input-DO-NOT-CREATE
KAIROS_OFFLINE_OUTBOX_DRAIN_DATA_NETWORK=offline-outbox-drain-data-network-MUST-BE-SUPPLIED
KAIROS_OFFLINE_OUTBOX_DRAIN_BUS_NETWORK=offline-outbox-drain-bus-network-MUST-BE-SUPPLIED
